[project]
name = "project-chimera"
version = "0.1.0"
description = "Agentic infrastructure factory for an autonomous influencer."
requires-python = ">=3.11"
dependencies = [
    "pytest",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
